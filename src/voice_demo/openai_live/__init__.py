"""Raw GPT-Live 1 weather agent with event-derived LangSmith tracing.

The CLI exposes this package as ``--backend openai-live``.  The Python package
uses an underscore because hyphens aren't valid in import names.
"""
